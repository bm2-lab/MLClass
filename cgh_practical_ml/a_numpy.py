import numpy as np


# basic operation
a = np.array([1,2,3,4], dtype=np.float32)
b = np.arange(9, dtype=np.float32).reshape((3,3))
c = np.arange(27, dtype=np.float32).reshape((3,3,3))


# linalg
# A = np.array([
#     [1, 3, 5],
#     [2, 5, 1],
#     [2, 3, 8]
# ], dtype=np.float)
#
# B = np.arange(1, 10, dtype=np.float).reshape((3,3))


# random

